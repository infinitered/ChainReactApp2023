import { Text, View } from "react-native"

export default function VenueScreen() {
  return (
    <View
      style={{
        justifyContent: "center",
        alignItems: "center",
        flex: 1,
      }}
    >
      <Text>Venue</Text>
    </View>
  )
}
